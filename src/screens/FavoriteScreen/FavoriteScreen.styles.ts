import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { 
    flex: 1, 
    backgroundColor: '#171010' 
  },
  empty: { 
    color: '#bbb', 
    textAlign: 'center', 
    marginTop: 40 
  },
});
